# Switch Pager on/off.

  [ "$negate" = "0" ] && [ "$cmd_pos" = "0" ] && 
    writeln "%d pager on." && pager=1 && return 0;
  [ "$negate" = "1" ] && [ "$cmd_pos" = "1" ] && 
    writeln "%d pager off." && pager=0 && return 0;

  return 1;
